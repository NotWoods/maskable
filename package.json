{
    "name": "maskable",
    "scripts": {
        "format": "prettier \"src/*.js\" \"*.{html,css,md}\" \"workbox-config.js\" --write",
        "build": "npm run prepare && npm run minify && npm run gzip && npm run sw",
        "prepare": "pika install",
        "sw": "workbox generateSW workbox-config.js",
        "minify": "find . -maxdepth 2 -iname \"*.js\" -exec terser --compress --mangle --module -o {} -- {} \\;",
        "gzip": "gzip -k9nr ."
    },
    "dependencies": {
        "dark-mode-toggle": "^0.2.5",
        "file-drop-element": "^0.2.0",
        "ganalytics": "^3.1.2"
    },
    "devDependencies": {
        "@pika/web": "^0.5.3",
        "prettier": "^1.18.2",
        "terser": "^4.2.1",
        "workbox-cli": "^4.3.1"
    },
    "prettier": {
        "singleQuote": true,
        "trailingComma": "all",
        "proseWrap": "always"
    }
}
